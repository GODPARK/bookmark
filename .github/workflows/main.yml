name: github-action-test

on:
  push:
    branches: [ feature/github-action ]

jobs:
  build:
    runs-on: [ self-hosted ]
    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: test
        run: ls /home/github/actions-runner/_work/bookmark/bookmark/

      - name: build
        uses: ./.github/actions/action

