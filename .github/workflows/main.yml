name: github-action-test

on:
  push:
    branches: [ feature/github-action ]

jobs:
  build:
    runs-on: [ self-hosted ]
    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: test
        run: ls -al /home/github/actions-runner/_work/bookmark/bookmark/

      - name: test2
        run: ls -al /home/github/actions-runner/_work/bookmark/bookmark/.github/ && ls -al /home/github/actions-runner/_work/bookmark/bookmark/.github/actions

      - name: build
        uses: ./.github/actions/action

